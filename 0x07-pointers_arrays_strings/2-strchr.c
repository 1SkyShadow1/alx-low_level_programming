#include "main.h"
/**
* _strchr - locates char in string
* @s: pointer to char
* @c: character
* Return: pointer
*/
char *_strchr(char *s, char c)
{
	int z;

	while (1)
	{
	z = *s++
	if (z == c)
	{
	return (s - 1);
	}
	if (z == 0)
	}
	return (NULL);
	}
	}
}
